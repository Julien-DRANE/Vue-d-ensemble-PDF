# PDF Overview

Petit outil statique pour afficher toutes les pages d'un PDF en mosaïque avec taille de vignettes ajustable.

## Lancer localement

Depuis ce dossier :

```bash
python3 -m http.server 8000
```

Puis ouvre `http://localhost:8000`.
